import { getGifs } from '../../src/helpers/GetGifs'

describe('Pruebas en GetGifs', () => {

    test('Debe de regresar un arreglo de gifs', async () => {

        // const gifs = await getGifs('Tacos')
        // console.log(gifs)
    })

})