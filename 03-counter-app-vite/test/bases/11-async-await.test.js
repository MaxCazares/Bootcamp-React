import { getGift } from "../../src/bases/11-async-await"

describe('pruebas en 11-async-await', () => {

    test('getGift debe de regresar el url de un gift', async () => {

        // const url = await getGift()
        // console.log(url);
        
        // expect(typeof url).toBe('string')
    })

})